apiVersion: v1
kind: Service
metadata:
  name: {{ include "sre-app-chart.fullname" . }}
  labels:
    {{- include "sre-app-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "sre-app-chart.selectorLabels" . | nindent 4 }}
